/*

Modified code from source:
http://docs.opencv.org/master/d1/dc5/tutorial_background_subtraction.html#gsc.tab=0

*/

//opencv
#include "opencv2/imgcodecs.hpp"
#include "opencv2/imgproc.hpp"
#include "opencv2/videoio.hpp"
#include <opencv2/highgui.hpp>
#include <opencv2/video.hpp>
//C++
#include <iostream>
#include <sstream>

using namespace cv;
using namespace std;

// Global variables
Mat frame; //normal frame
Mat fgMaskMOG2; //fg mask fg mask generated by MOG2 method
Mat thres;
Mat mask;
Ptr<BackgroundSubtractor> pMOG2; //MOG2 Background subtractor
int keyboard; //input from keyboard

// function prototype allows it to be called in the main function
// before it is actually defined
void processFeed(void);

int main(int argc, char** argv)
{
    //create windows
    namedWindow("Normal");
    namedWindow("Motion-senitive");
    //create Background Subtractor objects
    pMOG2 = createBackgroundSubtractorMOG2();
    processFeed();

    destroyAllWindows();
    return EXIT_SUCCESS;
}
void processFeed(void) {
    //VideoCapture capture(videoFilename);
    //open live feed from default camera.
    VideoCapture capture;
    capture.open(0);
    if(!capture.isOpened()){
        //error in opening the video input
        cerr << "Unable to open video feed." << endl;
        exit(EXIT_FAILURE);
    }
    //read input data. ESC or 'q' for quitting
    while( (char)keyboard != 'q' && (char)keyboard != 27 ){
        //read the current frame
        if(!capture.read(frame)) {
            cerr << "Unable to read next frame." << endl;
            cerr << "Exiting..." << endl;
            exit(EXIT_FAILURE);
        }

        //update the background model
        pMOG2->apply(frame, fgMaskMOG2);

        int colorMat[fgMaskMOG2.cols][fgMaskMOG2.rows];

        for(int y = 0; y < fgMaskMOG2.rows; y++) {
            for(int x = 0; x < fgMaskMOG2.cols; x++) {
                if(fgMaskMOG2.at<unsigned char>(x,y) == 255) colorMat[x][y] = 1;
                else colorMat[x][y] = 0;
            }
        }

        cout << colorMat[5][100] << endl;

        //get the frame number and write it on the current frame
        stringstream ss;
    //    rectangle(fgMaskMOG2, cv::Point(10, 2), cv::Point(100,20),
    //        cv::Scalar(255,255,255), -1);
        rectangle(fgMaskMOG2, cv::Point(10, 2), cv::Point(100,20),
            cv::Scalar(255,255,255), -1);

        ss << capture.get(CAP_PROP_POS_FRAMES);
        string frameNumberString = ss.str();

        putText(fgMaskMOG2, frameNumberString.c_str(), cv::Point(15, 15),
            FONT_HERSHEY_SIMPLEX, 0.5 , cv::Scalar(0,0,0));

        //show the current frame and the fg masks
        imshow("Normal", frame);
        imshow("Motion-senitive", fgMaskMOG2);
        //get the input from the keyboard
        keyboard = waitKey( 30 );
    }
    //delete capture object
    capture.release();
}